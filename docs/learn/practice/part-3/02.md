---
title: "例2 · 定义一个工具 + 测试"
---

# 例2 · 定义一个工具 + 测试

> 目标：用 `defineTool` 定一个工具，并给它写测试（正常 + 异常）。核心是 N-D-P-H + 统一出参 + vitest。

<!-- 双向导航: 回主线 -->
> 📚 **先用主线打底**：这一练对应 **[主线 第3部分](/learn/part-3/01)**——建议先读主线概念，再动手做。

## L1 最小工具（N-D-P-H） {#sec-1}

```ts
import { defineTool } from './tool'

export const listFilesTool = defineTool({
  name: 'list_files',
  description: '列出指定目录下的文件名。需要知道某文件夹有哪些文件时使用。',
  parameters: {
    type: 'object',
    properties: { dir: { type: 'string', description: '目录路径' } },
    required: ['dir'],
  },
  async handler({ dir }) {
    const fs = await import('fs/promises')
    try {
      return { ok: true, files: await fs.readdir(dir) }
    } catch (e: any) {
      return { ok: false, error: e.message }
    }
  },
})
```

## L2 规范出参 + 描述 {#sec-2}

> **出参统一** `{ ok, ... }`：成功带数据、失败带 `error`，**别抛异常崩**。**描述写清"何时用"**，模型才会用对。

```ts
export const countWordsTool = defineTool({
  name: 'count_words',
  description: '统计一段文字的单词数。当用户要求"数一下多少词"时使用。',
  parameters: { type: 'object', properties: { text: { type: 'string' } }, required: ['text'] },
  async handler({ text }) {
    if (typeof text !== 'string') return { ok: false, error: '需要字符串 text' }
    return { ok: true, count: text.trim().split(/\s+/).filter(Boolean).length }
  },
})
```

## L3 给工具写测试（vitest） {#sec-3}

```ts
import { expect, test } from 'vitest'

test('正常：列出目录', async () => {
  const r = await listFilesTool.handler({ dir: '.' })
  expect(r.ok).toBe(true)
  expect(Array.isArray(r.files)).toBe(true)
})

test('不存在目录：返回错误而不抛', async () => {
  const r = await listFilesTool.handler({ dir: '/no-such-dir-' + Date.now() })
  expect(r.ok).toBe(false)
  expect(typeof r.error).toBe('string')
})
```

## 变体练习（举一反三） {#sec-4}

用 N-D-P-H + 统一出参 + vitest，写：
1. **read_file**：读文件内容（异常：文件不存在）。
2. **sum_numbers**：把数字数组求和（异常：参数不是数组）。

## 通用套路：N-D-P-H + 统一出参 {#sec-5}

**N**ame · **D**escription（何时用）· **P**arameters · **H**andler。出参统一 `{ ok, ... }`；失败返回 `error` 而不抛；用 vitest 测正常 + 异常两路。
