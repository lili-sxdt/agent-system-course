---
title: "例1 · 最小模型调用与上下文"
---

# 例1 · 最小模型调用与上下文

> 目标：跑通第一次"给模型发消息、拿回复"，并理解上下文。从最小（L1）到现实中带上下文（L2），最后做个变体练习。

<!-- 双向导航: 回主线 -->
> 📚 **先用主线打底**：这一练对应 **[主线 第1部分](/learn/part-1/01)**——建议先读主线概念，再动手做。

## L1 最小能跑（只看关键） {#sec-1}

> Node 18+ 自带全局 `fetch`，无需安装。

```ts
const KEY = process.env.LLM_API_KEY!

const res = await fetch('https://api.deepseek.com/chat/completions', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${KEY}` },
  body: JSON.stringify({
    model: 'deepseek-chat',
    messages: [{ role: 'user', content: '用一句话介绍你自己' }],
  }),
})
const data = await res.json()
console.log(data.choices[0].message.content)
```

- **要搞懂**：请求体带 `model`（用哪个）和 `messages`（上下文）；回复里取 `choices[0].message`。
- **钥匙**：`process.env.LLM_API_KEY` 读环境变量（见 预备6）。

## L2 现实中：带上多轮上下文 {#sec-2}

> 一次"对话"不是一句，而是多轮历史。把历史按顺序放 `messages`。

```ts
const messages = [
  { role: 'system', content: '你是简洁的助手。' },
  { role: 'user', content: '我的项目是 Mini-Harness。' },
  { role: 'assistant', content: '好的，记住了。' },
  { role: 'user', content: '项目叫什么？' },     // 这一句靠前面历史才能答
]
const msg = await chat(messages)   // 见下面的 chat()
```

> **连贯性关键**：历史**顺序不能乱**，`system` 永远最前，工具结果也要带上（后续）。

## L3 一个可复用的 chat() {#sec-3}

```ts
async function chat(messages: any[], opts: { model?: string; temperature?: number } = {}) {
  const res = await fetch(`${process.env.LLM_BASE_URL}/chat/completions`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${process.env.LLM_API_KEY}` },
    body: JSON.stringify({ model: opts.model ?? 'deepseek-chat', messages, temperature: opts.temperature ?? 0.3 }),
  })
  return (await res.json()).choices[0].message
}
```

## 变体练习（举一反三） {#sec-4}

自己改一改、跑一跑：
1. **换模型**：把 `model` 换成另一个（如 `deepseek-chat` → 别的）。
2. **改温度**：`temperature` 设成 `0.9`，再设成 `0`，看回答风格差异。
3. **改上下文**：去掉中间那轮"assistant"历史，看模型能不能答对"项目叫什么"。

## 通用套路：上下文公式 {#sec-5}

**上下文 = system(规则) + 历史(多轮) + 工具结果(后面)。** 想让模型"知道"什么，就往 `messages` 里加什么；不加的它一概不知。
